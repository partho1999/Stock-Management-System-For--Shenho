import React from 'react';
import Layout from '../Layout/Layout';

const DashPage = () => {
    return (
        <Layout>
           <p>No data is here!</p> 
        </Layout>
    );
};

export default DashPage;